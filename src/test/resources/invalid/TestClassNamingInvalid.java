package test;

// This class is in test sources but named incorrectly — doesn't end in Test/SlowTest,
// doesn't start with Test or Abstract
public class BadlyNamedHelper {

    public void someMethod() {
    }
}
