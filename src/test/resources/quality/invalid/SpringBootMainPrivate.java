package quality.invalid;

@SpringBootApplication
public class SpringBootMainPrivate {

    private static void main(String[] args) {
        SpringApplication.run(SpringBootMainPrivate.class, args);
    }
}
